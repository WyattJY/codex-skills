# 表格 CSV 约定（建议）

- **一表一文件**：每个论文表格对应一个 CSV 文件，便于复用与审校。
- **第一行表头**：列名写全（尽量短）；后续每行是一条记录。
- **含逗号需加引号**：字段内容含 `,` 或换行时使用双引号包裹（CSV 标准写法）。
- **编码**：Windows 下 Excel/WPS 推荐 **UTF-8 with BOM**；换行建议 CRLF。
- **命名**：推荐 `table<章>_<序号>_<slug>.csv`，例如 `table5_2_backend_api_endpoints.csv`，便于自动匹配 `表5-2`。
