# Windows Mirror Reference

This folder preserves the previous `origin/main` Windows-oriented Claude skills mirror metadata and PowerShell tooling.

It is reference-only. Claude Code active skills are counted only from `../skills/<name>/SKILL.md`; this folder intentionally contains no `SKILL.md` files.

The previous Windows active `skills/` tree is preserved as `windows-active-skills-snapshot.tar.gz` so the remote history is not discarded while keeping the active Mac/Claude count clean.

The active Mac/Claude Code migration is:

- `69/69` Codex skills migrated to Claude Code.
- `14/14` Hermes local skills migrated to Claude Code.
- `1` Claude-native `wyatt-product-team` entrypoint.
- `84` active Claude Code skills installed under `/Users/jiangyu/.claude/skills`.
