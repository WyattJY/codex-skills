# Copy to ~/.codex/config.toml and replace <host>.

model_provider = "cliproxyapi"
model = "gpt-5-codex"
model_reasoning_effort = "xhigh"

[model_providers.cliproxyapi]
name = "cliproxyapi"
base_url = "http://<host>:8317/v1"
wire_api = "responses"
