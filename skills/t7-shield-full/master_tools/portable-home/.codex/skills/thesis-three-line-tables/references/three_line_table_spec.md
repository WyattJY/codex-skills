# 三线表排版要点（论文常用）

- **三条线**：仅保留表格顶线（加粗）、表头下分隔线（细）、表格底线（加粗）。
- **不画竖线**：左右边框、内部竖线均不显示；避免“网格表”观感。
- **表头**：加粗、居中；必要时可分两行（但尽量短）。
- **数字对齐**：纯数值列右对齐；文本列左对齐；“方法/类型”这类短字段可居中。
- **字体字号**：中文宋体，英文/数字 Times New Roman；常用 10.5pt（五号）；列较多时可用 9pt（小五）并切换横向页面。
- **长文本处理**：接口列表、长说明建议主动换行（例如用分号分隔项时按分号断行），避免把列撑得过宽。

