skills/hermes-home/imagegen/SKILL.md:14:- **Default built-in tool mode (preferred):** built-in `image_gen` tool for normal image generation, editing, and simple transparent-image requests. Does not require `OPENAI_API_KEY`.
skills/hermes-home/imagegen/SKILL.md:15:- **Fallback CLI mode:** `scripts/image_gen.py` CLI. Use when the user explicitly asks for the CLI/API/model path, or after the user explicitly confirms a true model-native transparency fallback with `gpt-image-1.5`. Requires `OPENAI_API_KEY`.
skills/hermes-home/imagegen/SKILL.md:30:- If the built-in tool fails or is unavailable, tell the user the CLI fallback exists and that it requires `OPENAI_API_KEY`. Proceed only if the user explicitly asks for that fallback.
skills/hermes-home/imagegen/SKILL.md:156:This likely needs true native transparency. The default built-in path uses a chroma-key background plus local removal, but true transparency requires the CLI fallback with gpt-image-1.5 because gpt-image-2 does not support background=transparent. It also requires OPENAI_API_KEY. Should I proceed with that CLI fallback?
skills/hermes-home/imagegen/SKILL.md:333:- `OPENAI_API_KEY` must be set for live API calls.
skills/hermes-home/imagegen/SKILL.md:334:- Do not ask the user for `OPENAI_API_KEY` when using the built-in `image_gen` tool.
skills/hermes-home/imagegen/SKILL.md:339:2. Set `OPENAI_API_KEY` as an environment variable in their system.
skills/hermes-home/imagegen/scripts/image_gen.py:70:    if os.getenv("OPENAI_API_KEY"):
skills/hermes-home/imagegen/scripts/image_gen.py:71:        print("OPENAI_API_KEY is set.", file=sys.stderr)
skills/hermes-home/imagegen/scripts/image_gen.py:74:        _warn("OPENAI_API_KEY is not set; dry-run only.")
skills/hermes-home/imagegen/scripts/image_gen.py:76:    _die("OPENAI_API_KEY is not set. Export it before running.")
skills/hermes-home/imagegen/references/cli.md:13:Real API calls require **network access** + `OPENAI_API_KEY`. `--dry-run` does not.
skills/hermes-home/imagegen/references/cli.md:40:Generate (requires `OPENAI_API_KEY` + network):
skills/mac-codex/.system/imagegen/SKILL.md:14:- **Default built-in tool mode (preferred):** built-in `image_gen` tool for normal image generation, editing, and simple transparent-image requests. Does not require `OPENAI_API_KEY`.
skills/mac-codex/.system/imagegen/SKILL.md:15:- **Fallback CLI mode:** `scripts/image_gen.py` CLI. Use when the user explicitly asks for the CLI/API/model path, or after the user explicitly confirms a true model-native transparency fallback with `gpt-image-1.5`. Requires `OPENAI_API_KEY`.
skills/mac-codex/.system/imagegen/SKILL.md:30:- If the built-in tool fails or is unavailable, tell the user the CLI fallback exists and that it requires `OPENAI_API_KEY`. Proceed only if the user explicitly asks for that fallback.
skills/mac-codex/.system/imagegen/SKILL.md:156:This likely needs true native transparency. The default built-in path uses a chroma-key background plus local removal, but true transparency requires the CLI fallback with gpt-image-1.5 because gpt-image-2 does not support background=transparent. It also requires OPENAI_API_KEY. Should I proceed with that CLI fallback?
skills/mac-codex/.system/imagegen/SKILL.md:333:- `OPENAI_API_KEY` must be set for live API calls.
skills/mac-codex/.system/imagegen/SKILL.md:334:- Do not ask the user for `OPENAI_API_KEY` when using the built-in `image_gen` tool.
skills/mac-codex/.system/imagegen/SKILL.md:339:2. Set `OPENAI_API_KEY` as an environment variable in their system.
skills/mac-codex/.system/imagegen/references/cli.md:13:Real API calls require **network access** + `OPENAI_API_KEY`. `--dry-run` does not.
skills/mac-codex/.system/imagegen/references/cli.md:40:Generate (requires `OPENAI_API_KEY` + network):
skills/mac-codex/.system/imagegen/scripts/image_gen.py:70:    if os.getenv("OPENAI_API_KEY"):
skills/mac-codex/.system/imagegen/scripts/image_gen.py:71:        print("OPENAI_API_KEY is set.", file=sys.stderr)
skills/mac-codex/.system/imagegen/scripts/image_gen.py:74:        _warn("OPENAI_API_KEY is not set; dry-run only.")
skills/mac-codex/.system/imagegen/scripts/image_gen.py:76:    _die("OPENAI_API_KEY is not set. Export it before running.")
skills/windows-codex-home/.system/imagegen/SKILL.md:14:- **Default built-in tool mode (preferred):** built-in `image_gen` tool for normal image generation, editing, and simple transparent-image requests. Does not require `OPENAI_API_KEY`.
skills/windows-codex-home/.system/imagegen/SKILL.md:15:- **Fallback CLI mode:** `scripts/image_gen.py` CLI. Use when the user explicitly asks for the CLI/API/model path, or after the user explicitly confirms a true model-native transparency fallback with `gpt-image-1.5`. Requires `OPENAI_API_KEY`.
skills/windows-codex-home/.system/imagegen/SKILL.md:30:- If the built-in tool fails or is unavailable, tell the user the CLI fallback exists and that it requires `OPENAI_API_KEY`. Proceed only if the user explicitly asks for that fallback.
skills/windows-codex-home/.system/imagegen/SKILL.md:156:This likely needs true native transparency. The default built-in path uses a chroma-key background plus local removal, but true transparency requires the CLI fallback with gpt-image-1.5 because gpt-image-2 does not support background=transparent. It also requires OPENAI_API_KEY. Should I proceed with that CLI fallback?
skills/windows-codex-home/.system/imagegen/SKILL.md:333:- `OPENAI_API_KEY` must be set for live API calls.
skills/windows-codex-home/.system/imagegen/SKILL.md:334:- Do not ask the user for `OPENAI_API_KEY` when using the built-in `image_gen` tool.
skills/windows-codex-home/.system/imagegen/SKILL.md:339:2. Set `OPENAI_API_KEY` as an environment variable in their system.
skills/windows-codex-home/.system/imagegen/references/cli.md:13:Real API calls require **network access** + `OPENAI_API_KEY`. `--dry-run` does not.
skills/windows-codex-home/.system/imagegen/references/cli.md:40:Generate (requires `OPENAI_API_KEY` + network):
skills/windows-codex-home/.system/imagegen/scripts/image_gen.py:70:    if os.getenv("OPENAI_API_KEY"):
skills/windows-codex-home/.system/imagegen/scripts/image_gen.py:71:        print("OPENAI_API_KEY is set.", file=sys.stderr)
skills/windows-codex-home/.system/imagegen/scripts/image_gen.py:74:        _warn("OPENAI_API_KEY is not set; dry-run only.")
skills/windows-codex-home/.system/imagegen/scripts/image_gen.py:76:    _die("OPENAI_API_KEY is not set. Export it before running.")
